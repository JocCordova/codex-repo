# Sketch notes

Drop rough sketch notes here as plain text or markdown.

The MVP planner can ingest a structured YAML request directly.
If you start from notes, convert notes into the request schema fields before generation.
